@using System.Security.Policy
@model Presentation.ViewModels.ApplicationUserViewModel
<body>
    @{
        Layout = "~/Views/Shared/_Layout.cshtml";
        ViewBag.Title = "WeCoin - Home";
        Styles.Render("~/Content/PostList.css");
    }
    <div class="container text-center" style="margin-top: 3%">
        <div class="row">


            <div class="col-sm-3">
                <div class="well">
                    <img src="/padrao.png" class="img-circle" height="65" width="65" alt="Avatar">
                    <p>@Html.ActionLink("Meu perfil", "Profile", "Home")</p>
                </div>

                <div class="well">
                    <p><a href="#">Interests</a></p>
                    <p>
                        <span class="label label-default">News</span>
                        <span class="label label-primary">W3Schools</span>
                        <span class="label label-success">Labels</span>
                        <span class="label label-info">Football</span>
                        <span class="label label-warning">Gaming</span>
                        <span class="label label-danger">Friends</span>
                    </p>
                </div>
                <div class="alert alert-success fade in">
                    <a href="#" class="close" data-dismiss="alert" aria-label="close">×</a>
                    <p><strong>Ey!</strong></p>
                    People are looking at your profile. Find out who.
                </div>
            </div>

            <div class="col-sm-6">
                @using (Html.BeginForm("CreateUserPost", "User"))
                {
                    @Html.AntiForgeryToken()
                    <form class="form-horizontal well" role="form">
                        <h4>O que é que há, velhinho?</h4>
                        <textarea class="form-control input-lg" name="Content" style="min-width: 100%" placeholder="Dá o papo"></textarea>
                        <input type="submit" value="Publicar" class="btn btn-primary pull-right" /><ul class="list-inline"><li><a href=""><i class="glyphicon glyphicon-upload"></i></a></li><li><a href=""><i class="glyphicon glyphicon-camera"></i></a></li><li><a href=""><i class="glyphicon glyphicon-map-marker"></i></a></li></ul>
                    </form>
                }


            </div>

            <div class="form-horizontal">
                <div class="col-sm-3">
                    <div class="thumbnail">
                        <p>MINERAR AQUI</p>
                    </div>
                </div>
            </div>

        </div>
    </div>



    @*<table class="table col-sm-9">*@
    @*<tr>
            <th>
                @Html.DisplayNameFor(model => model.ImgUrl)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Posts)
            </th>
        </tr>*@







    @*@Model.Friends*@
    @*@Model.Posts.First()*@

    @*@foreach (var item in Model.Friends)
        {
            foreach (var item2 in item.ToApplicationUser.Posts)
            {
                <tr>
                    <td>
                        <img src="@Url.Content(item2.Content)" style="width: 50px; height: 50px;" />
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item2.ImageUri)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item2.PostTime)
                    </td>
                </tr>
            }
        }*@
    @*</table>*@
</body>

@section Scripts
{
    <script>

    </script>
}